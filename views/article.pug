extends layout

block content
  div.articles
    div.article
      h2 #{title}
      div.date #{date} | #{author}
      div.article !{content}

    div.comments
      each comment in comments
        div.comment
          div.author #{comment.author}
          div.content !{comment.content}
